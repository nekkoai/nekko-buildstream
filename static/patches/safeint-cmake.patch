diff --git a/CMakeLists.txt b/CMakeLists.txt
index 369944c..f8e910c 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -26,4 +26,28 @@ add_subdirectory(Test/ClangTest)
 add_subdirectory(Test/GccTest)
 add_subdirectory(Test/MsvcTest)
 
-enable_testing()
\ No newline at end of file
+enable_testing()
+
+add_library(safeint INTERFACE)
+add_library(safeint::safeint ALIAS safeint)
+set(TARGET_HEADERS SafeInt.hpp safe_math.h safe_math_impl.h)
+set_target_properties(safeint PROPERTIES PUBLIC_HEADER "${TARGET_HEADERS}")
+set(version_config "${CMAKE_CURRENT_BINARY_DIR}/safeintConfigVersion.cmake")
+
+include(CMakePackageConfigHelpers)
+write_basic_package_version_file("${version_config}"
+    VERSION ${PROJECT_VERSION}
+    COMPATIBILITY SameMajorVersion)
+
+install(TARGETS safeint
+      EXPORT safeintConfig
+      PUBLIC_HEADER DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/safeint)
+export(TARGETS safeint NAMESPACE safeint:: FILE safeintTargets.cmake)
+set(CONFIG_LOC "${CMAKE_INSTALL_LIBDIR}/cmake/safeint")
+install(EXPORT safeintConfig
+    FILE safeintTargets.cmake
+    NAMESPACE safeint::
+    DESTINATION ${CONFIG_LOC})
+install (
+    FILES safeintConfig.cmake "${version_config}"
+    DESTINATION ${CONFIG_LOC})
diff --git a/safeintConfig.cmake b/safeintConfig.cmake
new file mode 100644
index 0000000..2f083ab
--- /dev/null
+++ b/safeintConfig.cmake
@@ -0,0 +1,2 @@
+include( CMakeFindDependencyMacro )
+include( "${CMAKE_CURRENT_LIST_DIR}/safeintTargets.cmake" )
