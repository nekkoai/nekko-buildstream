diff --git a/host/esperanto-tools-libs/CMakeLists.txt b/host/esperanto-tools-libs/CMakeLists.txt
index a4c6bd3..fe8e49e 100644
--- a/host/esperanto-tools-libs/CMakeLists.txt
+++ b/host/esperanto-tools-libs/CMakeLists.txt
@@ -68,14 +68,7 @@ endif()
 if(NOT TARGET elfio::elfio)
     add_library(elfio::elfio ALIAS elfio)
 endif()
-find_package(gflags REQUIRED)
-# Create gflags targets if they don't exist (fallback)
-if(NOT TARGET gflags::gflags)
-    add_library(gflags::gflags SHARED IMPORTED)
-    set_target_properties(gflags::gflags PROPERTIES
-        IMPORTED_LOCATION "/usr/local/lib/libgflags.so"
-        INTERFACE_INCLUDE_DIRECTORIES "/usr/local/include")
-endif()
+find_package(Gflags 0.7.1 REQUIRED)
 find_package(libcap REQUIRED)
 find_package(easy_profiler REQUIRED)
 
@@ -205,7 +198,6 @@ target_compile_definitions(etrt_static PUBLIC ETRT_STATIC_DEFINE)
 add_executable(server src/server/main.cpp)
 target_include_directories(server PRIVATE src)
 target_link_libraries(server 
-    PRIVATE 
         runtime::etrt
         deviceLayer::deviceLayer
         gflags::gflags
diff --git a/host/esperanto-tools-libs/include/runtime/Types.h b/host/esperanto-tools-libs/include/runtime/Types.h
index 8d25f5b..133b8c9 100644
--- a/host/esperanto-tools-libs/include/runtime/Types.h
+++ b/host/esperanto-tools-libs/include/runtime/Types.h
@@ -14,6 +14,7 @@
 #include <hostUtils/debug/StackException.h>
 
 #include <cstddef>
+#include <cstdint>
 #include <functional>
 #include <memory>
 #include <optional>
